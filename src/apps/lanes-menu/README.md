# Lanes Menu

A native macOS menu-bar launcher for persistent project lanes.

## Boundary

This module is a thin native client for the installed `lanes` JSON contracts. Lane state, services, logs, and actions belong to the CLI; Swift client,
view, packaging, and installation code belong to this directory. Keep process management and project discovery out of the app.

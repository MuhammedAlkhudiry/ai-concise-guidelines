# AI Usage Menu

A native macOS menu-bar app for at-a-glance Codex, OpenCode Go, and Claude usage.

Provider collection belongs to `Sources/AIUsageMenu/ProviderUsageClients.swift`; aggregation belongs to `AIUsageCollector.swift`. Keep authentication
in each provider's native store and never copy session material into app preferences.

# PostHog

- Use $posthog-cli for operations.
- For access, source `$SERVICE_CREDENTIALS_HOME/secrets.zsh` and check `POSTHOG_CLI_API_KEY`, `POSTHOG_CLI_HOST`, `POSTHOG_CLI_ORGANIZATION_ID`, and `POSTHOG_CLI_PROJECT_ID`; run `rtk posthog-cli login` to repair missing authentication.
